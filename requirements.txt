# ============================================
# cloud_amd_training — Dependencies
# Usage:
#   pip install -r requirements.txt          # 标准安装
#   bash run_training.sh                     # AMD Cloud 一键安装 + 训练
# ============================================

# Core ML
torch>=2.0.0
numpy>=1.24.0

# scipy — 不在此指定版本, 避免与 torcheeg 的 scipy<=1.10.1 约束冲突
# Python 3.12 需 scipy>=1.12.0, 需特殊处理:
#   pip install scipy --upgrade
#   pip install torcheeg --no-deps
# 脚本 run_training.sh 已自动处理, 见注释.

# TorchEEG (EEG 深度学习工具箱)
# 注意: Python 3.12 下需加 --no-deps 安装 (因 scipy 版本约束冲突)
#   pip install scipy --upgrade
#   pip install torcheeg --no-deps
# 或使用 run_training.sh 自动安装:
#   bash run_training.sh
torcheeg>=1.1.3

# ML / Metrics
scikit-learn>=1.2.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# t-SNE (特征可视化)
scikit-learn[extra]>=1.2.0

# Progress bar
tqdm>=4.65.0

# 可选: Jupyter notebook 集成
ipykernel>=6.0.0

# KaggleHub (DEAP 数据集下载)
kagglehub>=0.3.0

# 数据处理
pandas>=2.0.0
